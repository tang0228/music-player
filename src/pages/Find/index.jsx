import React, { } from 'react';
import "./index.less";

export default function Find(props) {
    
    return (
        <div className="find-container">
            {props.children}
        </div>
    )
}
